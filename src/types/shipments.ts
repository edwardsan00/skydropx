export type QuoterType = {
  from: string
  to: string
  weight: string
  length: string
  width: string
  height: string
}

export type TypeShipments = 'shipments' | 'parcels' | 'rates' | 'addresses'
